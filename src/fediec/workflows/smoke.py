from __future__ import annotations


def run_smoke() -> None:
    raise NotImplementedError(
        "smoke is not yet implemented: it depends on preprocess/prepare/run, "
        "none of which are built yet."
    )
